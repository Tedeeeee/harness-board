Read the skill file at `skills/ship/SKILL.md` in this project and follow its instructions exactly.

Context: The user wants to ship — detect and merge base branch, run tests, review diff, bump version, update changelog, commit, push, and create a PR.

If the user provides arguments: $ARGUMENTS
