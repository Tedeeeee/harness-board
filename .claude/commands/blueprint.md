Read the skill file at `skills/blueprint/SKILL.md` in this project and follow its instructions exactly.

Context: The user wants to turn an objective into a step-by-step construction plan for multi-session, multi-agent engineering projects. Each step should have a self-contained context brief so a fresh agent can execute it cold.

If the user provides arguments: $ARGUMENTS
